import { WritableComputedRef } from 'vue'
import { IBlockItem } from '@/types'

/**
 * @author lihh
 * @description 进行元素的复制
 * @param allBlockItem 表示所有的元素
 */
export const useBlockV = (allBlockItem: WritableComputedRef<IBlockItem[]>) => {

}
